import { BoxShadowColorDirective } from './box-shadow-color.directive';

describe('BoxShadowColorDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxShadowColorDirective();
    expect(directive).toBeTruthy();
  });
});
