import { RoundCountPipe } from './round-count.pipe';

describe('RoundCountPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundCountPipe();
    expect(pipe).toBeTruthy();
  });
});
